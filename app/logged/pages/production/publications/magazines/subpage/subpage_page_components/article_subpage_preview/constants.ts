/** 228 × 297 mm portrait (magazine page proportion). */
export const PAGE_ASPECT = "228 / 297";

/** Title/subtitle band — extra horizontal and vertical breathing room. */
export const MAGAZINE_HEADER_PAD_CLASS = "px-10 py-5";

/**
 * Body content inset: matches chunk text padding, with `pt-12` below the
 * black header. Grid overlays and add/delete-image cells share this box.
 */
export const MAGAZINE_BODY_INSET_CLASS = "px-10 pt-8 pb-12";
