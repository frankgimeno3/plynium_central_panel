/** Matches synthetic rows created without a portal article (`PublicationArticleService`). */
export const STANDALONE_PUBLICATION_ARTICLE_PREFIX = "local_standalone:";

export const NON_DELETABLE_CHUNK_FORMATS = new Set(["title", "subtitle"]);

export const PAGE_THUMB_ASPECT = "228 / 297";
